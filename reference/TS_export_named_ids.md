# Export identities for each cluster, which are not numerical

Export identities for each cluster, which are not numerical

## Usage

``` r
TS_export_named_ids(seurat_object)
```

## Arguments

- seurat_object:

  The Seurat object you wish to retrieve the identities from. Writes to
  working directory.
