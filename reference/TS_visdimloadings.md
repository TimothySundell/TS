# Plot gene contribution to each principal component

Plot gene contribution to each principal component

## Usage

``` r
TS_visdimloadings(seurat_object = get(default_seurat_object), dims = 1:10)
```

## Arguments

- seurat_object:

  The Seurat object you want to plot

- dims:

  What principal components to plot, default = 10
