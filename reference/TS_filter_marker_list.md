# Filter list of markers from FindMarkers function, needs to be output into new df

Filter list of markers from FindMarkers function, needs to be output
into new df

## Usage

``` r
TS_filter_marker_list(df, markers_to_remove)
```

## Arguments

- df:

  The dataframe you wish to filter

- markers_to_remove:

  Vector of markers to remove
