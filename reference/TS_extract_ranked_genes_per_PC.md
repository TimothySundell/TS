# Extract an ordered list of feature importance for each of the first 50 principal components

Extract an ordered list of feature importance for each of the first 50
principal components

## Usage

``` r
TS_extract_ranked_genes_per_PC(seurat_object)
```

## Arguments

- seurat_object:

  The Seurat object you wish to extract PCA feature loadings from
